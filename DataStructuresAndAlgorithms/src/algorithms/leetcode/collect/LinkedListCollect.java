package algorithms.leetcode.collect;

/**
 * Version 1.0
 * Created by lll on 2020-05-06.
 * Description
 * <pre>
 *   链表相关算法汇总
 *     1、链表的逆序，分组逆序，
 *        逆序的解法：都是找到， fromPreNode, fromNode， toNode，toNextNode；
 *
 *     2、链表删除某一个节点、倒序删除某一个节点、是否有环
 *         这种方式的解法：都是采用快慢指针找到要删除的节点。
 *
 *     3、链表插入(头插、尾插)、Lru
 *         3.1 插入都是采用哨兵方式： 建立一个空的哨兵(header) -> next(才是真正的数据)
 *         3.2 lru 的步骤
 *            3.2.1 定义头尾指针：
 *            3.2.2 就是每次访问一个元素后都将元素移到尾指针，每次获取元素的时候都从尾指针获取
 *
 *
 *     4、合并两个有序链表：
 *
 *
 *     5、链表之间的排序：
 *
 *
 *
 * </pre>
 * copyright generalray4239@gmail.com
 */
public class LinkedListCollect {
}
